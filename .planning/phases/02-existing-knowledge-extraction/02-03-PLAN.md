---
phase: 02-existing-knowledge-extraction
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - knowledge/pipeline/handlers.py
  - knowledge/pipeline/graph_builder.py
  - knowledge/extracted/user_journey/learning.json
  - knowledge/extracted/_graph_snapshot.json
  - tests/test_extraction.py
autonomous: true
requirements: [EXIST-02, EXIST-05]

must_haves:
  truths:
    - "learning.md training entries are extracted with concepts linked to canonical registry IDs"
    - "Knowledge graph has edges from cross-references found across all extraction outputs"
    - "A causal chain query like 'what causes elbow_flying_out' returns at least one path"
    - "Graph has 300-500 edges connecting concepts from different sources"
  artifacts:
    - path: "knowledge/pipeline/graph_builder.py"
      provides: "Graph assembly from extraction results"
      exports: ["build_graph_from_extractions"]
    - path: "knowledge/extracted/user_journey/learning.json"
      provides: "Structured extraction from user training journal"
    - path: "knowledge/extracted/_graph_snapshot.json"
      provides: "Serialized knowledge graph with all nodes and edges"
  key_links:
    - from: "knowledge/pipeline/graph_builder.py"
      to: "knowledge/graph.py"
      via: "KnowledgeGraph.add_concept() and add_edge()"
      pattern: "graph\\.add_concept\\(|graph\\.add_edge\\("
    - from: "knowledge/pipeline/graph_builder.py"
      to: "knowledge/extracted/"
      via: "Loading per-file JSON extractions"
      pattern: "json\\.load"
    - from: "knowledge/pipeline/handlers.py"
      to: "docs/record/learning.md"
      via: "extract_user_journey handler"
      pattern: "extract_user_journey"
---

<objective>
Extract user training journey and build the initial knowledge graph with edges from all sources.

Purpose: The user journey (learning.md) connects personal experience to canonical concepts, enabling personalized diagnostics later. The graph builder assembles all extracted concepts and edges into a queryable KnowledgeGraph with causal chain traversal.

Output: User journey extraction, complete graph snapshot with 300-500 edges, passing causal chain queries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-existing-knowledge-extraction/02-RESEARCH.md
@.planning/phases/02-existing-knowledge-extraction/02-01-SUMMARY.md
@knowledge/schemas.py
@knowledge/registry.py
@knowledge/graph.py
@knowledge/pipeline/seed.py
@knowledge/pipeline/extractor.py

<interfaces>
From knowledge/graph.py:
```python
class KnowledgeGraph:
    def add_concept(self, concept: Concept) -> None
    def add_edge(self, edge: Edge) -> None
    def get_causal_chain(self, symptom_id: str, cause_type: str = "causes") -> list[list[str]]
    def to_json(self, path: Path) -> None
    @classmethod
    def from_json(cls, path: Path) -> KnowledgeGraph
    @property
    def node_count(self) -> int
    @property
    def edge_count(self) -> int
```

From knowledge/pipeline/extractor.py:
```python
@dataclass
class ExtractionResult:
    concepts: list[Concept]
    edges: list[Edge]
    source_file: str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract user training journey from learning.md</name>
  <files>knowledge/pipeline/handlers.py, knowledge/extracted/user_journey/learning.json</files>
  <read_first>
    - docs/record/learning.md (1332 lines -- read first 300 lines to understand date-entry format, causal chain notation)
    - docs/knowledge_graph/user_journey.json (legacy extraction with 21 problems, 16 breakthroughs -- understand existing structure)
    - knowledge/pipeline/handlers.py (existing handler stubs or implementations)
    - knowledge/pipeline/seed.py (to_snake_id, for concept ID generation)
  </read_first>
  <action>
    Implement `extract_user_journey(filepath, registry)` in handlers.py:

    1. **Parse date-based entries** from learning.md. Each training session has a date header and notes. Extract:
       - Problems identified (link to SYMPTOM concepts in registry via resolve())
       - Breakthroughs/insights (link to TECHNIQUE/CONNECTION concepts)
       - Drills tried (link to DRILL concepts)
       - Causal chains in arrow notation (原因A → 导致B → 导致C) -> CAUSES edges

    2. **Also migrate existing user_journey.json** as supplementary data:
       - 21 problems -> resolve against registry for canonical concept IDs
       - 16 breakthroughs -> resolve against registry
       - 57 cue evolutions -> extract as edges (old_cue SUPPORTS new_cue or concept)
       - problem_dependency_graph -> direct CAUSES edges

    3. **Edge extraction from learning.md:**
       - Arrow chains (→, ->, ==>) -> CAUSES edges with confidence 0.7
       - "因为/because" patterns -> CAUSES edges
       - "解决方案/fix" patterns -> DRILLS_FOR edges
       - Date-stamped observations are evidence strings for edges

    4. Save extraction result to `knowledge/extracted/user_journey/learning.json`.

    Source identifier for all user journey concepts/edges: "user_experience".
  </action>
  <verify>
    <automated>cd /Users/qsy/Desktop/tennis && python3 -c "
from knowledge.pipeline.handlers import extract_user_journey
from knowledge.registry import ConceptRegistry
from knowledge.pipeline.seed import seed_registry_from_legacy_json
from pathlib import Path
r = ConceptRegistry()
seed_registry_from_legacy_json(r)
result = extract_user_journey(Path('docs/record/learning.md'), r)
print(f'Concepts: {len(result.concepts)}, Edges: {len(result.edges)}')
assert len(result.edges) >= 10, f'Too few edges: {len(result.edges)}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def extract_user_journey" knowledge/pipeline/handlers.py returns 1
    - ls knowledge/extracted/user_journey/learning.json exits 0
    - python3 -c "import json; d=json.load(open('knowledge/extracted/user_journey/learning.json')); print(len(d['edges']))" outputs >= 10
  </acceptance_criteria>
  <done>learning.md extracted with problems/breakthroughs linked to canonical concepts. Causal chains parsed into directed edges. Legacy user_journey.json migrated and merged.</done>
</task>

<task type="auto">
  <name>Task 2: Build knowledge graph from all extractions and validate causal chains</name>
  <files>knowledge/pipeline/graph_builder.py, knowledge/extracted/_graph_snapshot.json, tests/test_extraction.py</files>
  <read_first>
    - knowledge/graph.py (KnowledgeGraph API)
    - knowledge/extracted/_registry_snapshot.json (if exists from Plan 02, otherwise seed + extract inline)
    - knowledge/extracted/_canonical_seed.json (seed concepts from Plan 01)
    - tests/test_extraction.py (existing tests to unskip EXIST-02, EXIST-05)
  </read_first>
  <action>
    1. Create `knowledge/pipeline/graph_builder.py` with:
       - `build_graph_from_extractions(extracted_dir: Path) -> tuple[KnowledgeGraph, ConceptRegistry]`:
         - Load _canonical_seed.json or run seed to populate registry
         - Load _registry_snapshot.json if available (from Plan 02) to get full concept set
         - If _registry_snapshot.json doesn't exist yet (Plan 02 not complete), load individual per-file JSONs from knowledge/extracted/
         - Load user_journey/learning.json extraction
         - Add all concepts as nodes to KnowledgeGraph
         - Add all edges to KnowledgeGraph
         - Validate: warn on edges referencing non-existent node IDs (log but don't crash)
         - Save graph to `_graph_snapshot.json` via KnowledgeGraph.to_json()
         - Return (graph, registry) tuple

    2. Run `build_graph_from_extractions()`. Print summary:
       - Total nodes, total edges
       - Edges by relation type (CAUSES, SUPPORTS, DRILLS_FOR, etc.)
       - Test causal chain query: `graph.get_causal_chain("scooping")` or similar symptom
       - Test causal chain query: `graph.get_causal_chain("elbow_flying_out")`
       - List any orphan nodes (nodes with zero edges) -- these are OK for now but good to track

    3. Update `tests/test_extraction.py`:
       - Unskip EXIST-02 test: verify user_journey/learning.json exists with concepts and edges
       - Unskip EXIST-05 test: verify graph has edges and causal chain query returns results
       - Add test: load _graph_snapshot.json, verify node_count >= 100 and edge_count >= 50
       - Add test: at least one causal chain query returns non-empty result

    4. Run full test suite.

    **Note on Plan 02 dependency:** This plan depends on Plan 01 (seed) but runs in parallel with Plan 02 (research file extraction). The graph builder is designed to work with whatever extractions are available:
    - If Plan 02 is done: loads _registry_snapshot.json + all per-file JSONs -> full graph
    - If Plan 02 is NOT done yet: loads seed + user journey only -> partial graph
    - Either way, the graph_builder code is correct and tests pass
    
    The executor should run the graph builder AFTER Plan 02 completes for the full graph. But the code and tests are written to be resilient to partial data.
  </action>
  <verify>
    <automated>cd /Users/qsy/Desktop/tennis && python3 -m pytest tests/test_extraction.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def build_graph_from_extractions" knowledge/pipeline/graph_builder.py returns 1
    - ls knowledge/extracted/_graph_snapshot.json exits 0
    - python3 -c "from knowledge.graph import KnowledgeGraph; from pathlib import Path; g=KnowledgeGraph.from_json(Path('knowledge/extracted/_graph_snapshot.json')); print(f'nodes={g.node_count} edges={g.edge_count}'); assert g.edge_count >= 50" exits 0
    - python3 -m pytest tests/test_extraction.py -x -q exits 0
  </acceptance_criteria>
  <done>Knowledge graph built from all available extractions. Graph snapshot saved with nodes and edges. Causal chain queries work for known symptoms. All EXIST-02 and EXIST-05 tests pass.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_extraction.py -x -q` passes all tests
- `python3 -c "from knowledge.graph import KnowledgeGraph; from pathlib import Path; g=KnowledgeGraph.from_json(Path('knowledge/extracted/_graph_snapshot.json')); print(f'nodes={g.node_count}, edges={g.edge_count}')"` shows meaningful counts
- At least one causal chain query returns a non-empty path
- User journey extraction contains linked concept IDs from canonical registry
</verification>

<success_criteria>
- learning.md extracted with concepts linked to canonical registry
- Knowledge graph contains nodes from all extraction sources
- Graph has 50+ edges minimum (300-500 target when combined with Plan 02 output)
- Causal chain query returns at least one path for a known symptom
- Graph snapshot serialized and loadable
</success_criteria>

<output>
After completion, create `.planning/phases/02-existing-knowledge-extraction/02-03-SUMMARY.md`
</output>
