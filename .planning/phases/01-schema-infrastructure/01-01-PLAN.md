---
phase: 01-schema-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knowledge/__init__.py
  - knowledge/schemas.py
  - tests/test_knowledge_schemas.py
  - pyproject.toml
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "A Concept can be instantiated with all required fields and serialized to/from JSON"
    - "An Edge enforces typed directed relationships with a fixed RelationType enum"
    - "A DiagnosticChain models branching diagnostic logic with ordered check steps"
    - "Invalid concept IDs (uppercase, spaces, special chars) are rejected by validation"
  artifacts:
    - path: "knowledge/__init__.py"
      provides: "Package init exporting all schema classes"
      contains: "from knowledge.schemas import"
    - path: "knowledge/schemas.py"
      provides: "Pydantic v2 models: Concept, Edge, DiagnosticChain, enums"
      exports: ["Concept", "Edge", "DiagnosticChain", "DiagnosticStep", "ConceptType", "RelationType", "SourceId"]
      min_lines: 80
    - path: "tests/test_knowledge_schemas.py"
      provides: "Unit tests for all three models plus validation edge cases"
      min_lines: 60
  key_links:
    - from: "tests/test_knowledge_schemas.py"
      to: "knowledge/schemas.py"
      via: "import and instantiate all models"
      pattern: "from knowledge.schemas import"
---

<objective>
Define all Pydantic v2 data models for the knowledge engineering system: Concept, Edge, and DiagnosticChain, plus supporting enums. Create the knowledge/ package with pytest configuration.

Purpose: These schemas are the contract that all downstream extraction, graph assembly, and VLM diagnostic phases build against. Getting them right here prevents cascading rework.
Output: knowledge/schemas.py with validated models, tests proving roundtrip serialization, package init.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create knowledge package with Pydantic schemas and enums</name>
  <files>knowledge/__init__.py, knowledge/schemas.py, pyproject.toml</files>
  <read_first>
    - .planning/phases/01-schema-infrastructure/01-RESEARCH.md (Pattern 1, 2, 3 for exact field definitions)
    - pyproject.toml (to add pytest config section)
  </read_first>
  <behavior>
    - test_concept_roundtrip: Concept with all fields -> model_dump_json -> model_validate_json -> equal
    - test_concept_id_validation: "Hip Rotation" (uppercase) raises ValidationError; "hip_rotation" passes
    - test_concept_id_rejects_spaces: "hip rotation" raises ValidationError
    - test_concept_defaults: Concept with only required fields has empty lists for aliases, vlm_features, muscles_involved
    - test_edge_validation: Edge with RelationType.CAUSES roundtrips correctly
    - test_edge_all_relation_types: All 7 RelationType values can be used in Edge
    - test_diagnostic_chain: DiagnosticChain with check_sequence of DiagnosticSteps roundtrips
    - test_diagnostic_chain_priority_bounds: priority=0 raises ValidationError, priority=6 raises ValidationError
    - test_diagnostic_chain_id_prefix: id must start with "dc_", "bad_id" raises ValidationError
  </behavior>
  <action>
    1. Create knowledge/__init__.py with exports:
       ```python
       from knowledge.schemas import (
           Concept, ConceptType, SourceId,
           Edge, RelationType,
           DiagnosticStep, DiagnosticChain,
       )
       ```

    2. Create knowledge/schemas.py with these exact models (from research Pattern 1-3):

       Enums:
       - ConceptType(str, Enum): TECHNIQUE, BIOMECHANICS, DRILL, SYMPTOM, MENTAL_MODEL, CONNECTION
       - SourceId(str, Enum): FTT="ftt", TPA="tpa", FEEL_TENNIS="feel_tennis", BIOMECHANICS_BOOK="biomechanics_book", USER_EXPERIENCE="user_experience"
       - RelationType(str, Enum): CAUSES, PREVENTS, REQUIRES, CONTRADICTS, SUPPORTS, DRILLS_FOR, VISIBLE_AS

       Models:
       - Concept: id (Field pattern=r'^[a-z][a-z0-9_]*$'), name (str), name_zh (str), aliases (list[str] default=[]), category (ConceptType), sources (list[str] default=[]), description (str), vlm_features (list[str] default=[]), muscles_involved (list[str] default=[]), active_or_passive (str | None = None), confidence (float default=1.0 ge=0.0 le=1.0)
       - Edge: source_id (pattern=r'^[a-z][a-z0-9_]*$'), target_id (same pattern), relation (RelationType), confidence (float ge=0.0 le=1.0 default=1.0), evidence (str), source_file (str), resolution (str | None = None)
       - DiagnosticStep: check (str), check_zh (str), if_true (str), if_false (str | None = None)
       - DiagnosticChain: id (pattern=r'^dc_[a-z][a-z0-9_]*$'), symptom (str), symptom_zh (str), symptom_concept_id (str), check_sequence (list[DiagnosticStep]), root_causes (list[str]), drills (list[str]), priority (int ge=1 le=5), vlm_frame (str | None = None)

    3. Add to pyproject.toml:
       ```toml
       [tool.pytest.ini_options]
       testpaths = ["tests"]
       ```

    4. Create tests/test_knowledge_schemas.py with all tests from <behavior> block.
       Use exact test data from research Code Examples section (hip_rotation concept, arm_lag edge, dc_arm_driven_hitting chain).
  </action>
  <verify>
    <automated>cd /Users/qsy/Desktop/tennis && python -m pytest tests/test_knowledge_schemas.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class Concept" knowledge/schemas.py
    - grep -q "class Edge" knowledge/schemas.py
    - grep -q "class DiagnosticChain" knowledge/schemas.py
    - grep -q "class ConceptType" knowledge/schemas.py
    - grep -q "class RelationType" knowledge/schemas.py
    - grep -q "class DiagnosticStep" knowledge/schemas.py
    - grep -q "pattern=r" knowledge/schemas.py (ID validation regex present)
    - grep -q "from knowledge.schemas import" knowledge/__init__.py
    - grep -q "testpaths" pyproject.toml
    - python -m pytest tests/test_knowledge_schemas.py -x passes with 9+ tests
  </acceptance_criteria>
  <done>All three Pydantic models (Concept, Edge, DiagnosticChain) instantiate, validate IDs via regex, reject invalid inputs, and roundtrip through JSON. Package importable as `from knowledge import Concept`.</done>
</task>

</tasks>

<verification>
- `python -c "from knowledge import Concept, Edge, DiagnosticChain; print('OK')"` prints OK
- `python -m pytest tests/test_knowledge_schemas.py -x -v` all green with 9+ tests
- `python -c "from knowledge.schemas import ConceptType, RelationType, SourceId; print(len(ConceptType), len(RelationType))"` prints "6 7"
</verification>

<success_criteria>
- knowledge/ package exists and is importable
- All three core models (Concept, Edge, DiagnosticChain) have Pydantic v2 validation
- Concept IDs enforce snake_case via regex pattern
- DiagnosticChain IDs enforce dc_ prefix
- All 7 RelationType enum values defined
- pytest configuration added to pyproject.toml
- 9+ tests pass covering instantiation, validation, roundtrip, and error cases
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-infrastructure/01-01-SUMMARY.md`
</output>
