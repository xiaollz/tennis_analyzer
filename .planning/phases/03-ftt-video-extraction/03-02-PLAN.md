---
phase: 03-ftt-video-extraction
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - knowledge/extracted/ftt_videos/*.json
  - knowledge/state/ftt_video_state.json
  - scripts/extract_existing_ftt_videos.py
autonomous: true
requirements: [FTT-02]

must_haves:
  truths:
    - "All 33 unique already-analyzed FTT videos have structured concept JSON files"
    - "Each extracted JSON contains real tennis concepts (not video-title placeholders)"
    - "Concepts have valid snake_case IDs, English+Chinese names, and proper categories"
    - "Edges connect video concepts to existing registry concepts"
    - "Video state file shows all 33 analyzed videos as 'extracted'"
  artifacts:
    - path: "knowledge/extracted/ftt_videos/"
      provides: "33 per-video JSON extraction files"
      contains: "Concept and Edge objects from analysis text"
    - path: "scripts/extract_existing_ftt_videos.py"
      provides: "Runnable extraction script"
    - path: "knowledge/state/ftt_video_state.json"
      provides: "Updated state with 33 videos marked 'extracted'"
  key_links:
    - from: "scripts/extract_existing_ftt_videos.py"
      to: "knowledge/pipeline/video_concept_extractor.py"
      via: "extract_from_existing_markdown + extract_concepts_from_analysis"
      pattern: "extract_from_existing_markdown|extract_concepts_from_analysis"
    - from: "scripts/extract_existing_ftt_videos.py"
      to: "knowledge/pipeline/video_state.py"
      via: "state checkpoint after each video"
      pattern: "mark_video|save_state"
---

<objective>
Re-extract structured concepts from the 35 existing FTT video analyses (33 unique after dedup). These analyses already exist as rich Markdown in docs/research/09_ftt_videos_{1,2,3}.md -- no API calls needed.

Purpose: The current extracted JSON files are shallow (just video titles as concepts with 0.6 confidence). This plan replaces them with real structured concepts, edges, and diagnostic chains parsed from the comprehensive analysis text.

Output: 33 per-video JSON files with real concepts, updated video state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ftt-video-extraction/03-RESEARCH.md
@.planning/phases/03-ftt-video-extraction/03-01-SUMMARY.md

@knowledge/schemas.py
@knowledge/registry.py
@knowledge/pipeline/video_state.py
@knowledge/pipeline/video_concept_extractor.py
@docs/research/09_ftt_videos_1.md
@docs/research/09_ftt_videos_2.md
@docs/research/09_ftt_videos_3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract concepts from 33 existing video analyses</name>
  <files>scripts/extract_existing_ftt_videos.py, knowledge/extracted/ftt_videos/*.json, knowledge/state/ftt_video_state.json</files>
  <action>
Create `scripts/extract_existing_ftt_videos.py` that:

1. Loads the ConceptRegistry from `knowledge/extracted/_registry_snapshot.json` (297 existing concepts). Use:
   ```python
   registry = ConceptRegistry()
   for c_data in json.loads(snapshot_path.read_text()):
       registry.add(Concept(**c_data))
   ```

2. Calls `extract_from_existing_markdown()` on each of the 3 analysis files:
   - `docs/research/09_ftt_videos_1.md`
   - `docs/research/09_ftt_videos_2.md`
   - `docs/research/09_ftt_videos_3.md`
   Collects all per-video sections. Deduplicates by video_id (files 2 and 3 share gehRK0Y6AdQ and 9NirSL59an0).

3. For each unique video section, calls `extract_concepts_from_analysis(content, video_id, title, registry)` to produce Concept, Edge, and DiagnosticChain objects.

4. Saves each video's extraction to `knowledge/extracted/ftt_videos/{video_id}.json` using `save_video_extraction()`. This REPLACES the old shallow title-only JSON files.

5. After each video extraction, calls `mark_video(state, video_id, "extracted", extracted_file=...)` and `save_state()` for checkpointing.

6. Prints summary: total videos processed, total new concepts, total edges, total diagnostic chains.

Run the script:
```bash
cd /Users/qsy/Desktop/tennis && python scripts/extract_existing_ftt_videos.py
```

After running, verify:
- At least 33 JSON files exist in `knowledge/extracted/ftt_videos/`
- Each JSON has more than just a video-title concept (real concepts with descriptions)
- Video state shows 33 videos with status="extracted"

If extraction quality is poor for some videos (analysis text format varies), add fallback logic in the extractor: if the structured sections (Part 2, Part 4, etc.) are not found, extract bold terms and concept-like patterns from the full text as a degraded but still useful extraction.
  </action>
  <verify>
    <automated>cd /Users/qsy/Desktop/tennis && python scripts/extract_existing_ftt_videos.py 2>&1 | tail -30 && echo "---" && python3 -c "
import json, os
d = 'knowledge/extracted/ftt_videos'
files = [f for f in os.listdir(d) if f.endswith('.json')]
print(f'JSON files: {len(files)}')
total_concepts = 0
for f in files:
    data = json.load(open(os.path.join(d, f)))
    total_concepts += len(data.get('concepts', []))
print(f'Total concepts across all files: {total_concepts}')
state = json.load(open('knowledge/state/ftt_video_state.json'))
extracted = sum(1 for v in state['videos'].values() if v['status'] == 'extracted')
print(f'Videos with status=extracted: {extracted}')
"</automated>
  </verify>
  <done>
    - 33+ JSON files in knowledge/extracted/ftt_videos/ with real concept data
    - Total concepts across all files > 50 (not just 33 video titles)
    - Video state shows 33 videos as "extracted"
    - No API calls were made (pure text extraction)
  </done>
</task>

</tasks>

<verification>
- `ls knowledge/extracted/ftt_videos/*.json | wc -l` -- at least 33 files
- `python3 -c "import json; d=json.load(open('knowledge/extracted/ftt_videos/tzgAWr3QGv0.json')); print(len(d['concepts']), d['concepts'][0]['name'] if d['concepts'] else 'EMPTY')"` -- shows real concept names, not "7 Steps for a Sinner-Like Forehand"
- `python3 -c "import json; s=json.load(open('knowledge/state/ftt_video_state.json')); print(sum(1 for v in s['videos'].values() if v['status']=='extracted'))"` -- outputs 33
</verification>

<success_criteria>
- All 33 unique already-analyzed videos have JSON files with real structured concepts
- Each JSON file contains actual tennis concepts (technique, biomechanics, drill, symptom types)
- Video state correctly reflects all 33 as "extracted"
- Registry concept IDs are properly resolved for existing concepts (no unnecessary duplicates)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ftt-video-extraction/03-02-SUMMARY.md`
</output>
