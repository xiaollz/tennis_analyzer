---
phase: 06-secondary-sources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knowledge/pipeline/secondary_videos.py
  - knowledge/state/tomallsopp_video_state.json
  - knowledge/state/feeltennis_video_state.json
  - tests/test_secondary_state.py
autonomous: true
requirements:
  - SEC-01
  - SEC-03

must_haves:
  truths:
    - "TomAllsopp forehand-relevant videos are identified and stored in a state file"
    - "Feel Tennis forehand-relevant videos are identified and stored in a state file"
    - "Cross-channel duplicate (1-g1OD8gh-I) is excluded from secondary lists"
    - "Each state file has the same schema as ftt_video_state.json"
  artifacts:
    - path: "knowledge/pipeline/secondary_videos.py"
      provides: "Curated video lists for both channels + state generation"
      exports: ["TOMALLSOPP_FOREHAND_VIDEOS", "FEELTENNIS_FOREHAND_VIDEOS", "generate_tomallsopp_state", "generate_feeltennis_state"]
    - path: "knowledge/state/tomallsopp_video_state.json"
      provides: "TomAllsopp channel state with ~45 videos, all status=pending"
    - path: "knowledge/state/feeltennis_video_state.json"
      provides: "Feel Tennis channel state with ~45 videos, all status=pending"
    - path: "tests/test_secondary_state.py"
      provides: "Tests for state generation, schema validation, dedup"
  key_links:
    - from: "knowledge/pipeline/secondary_videos.py"
      to: "knowledge/pipeline/video_state.py"
      via: "Uses VideoEntry TypedDict and save_state/load_state"
      pattern: "from knowledge.pipeline.video_state import"
---

<objective>
Curate forehand-relevant video subsets from TomAllsopp (~45) and Feel Tennis (~45) channels and create state tracking files.

Purpose: Establishes the curated video lists that Plans 02 and 03 will process through the Gemini API. Title-keyword filtering from RESEARCH.md yt-dlp enumeration.
Output: Two state JSON files + a Python module with hardcoded curated video lists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-secondary-sources/06-RESEARCH.md

@knowledge/pipeline/video_state.py
@knowledge/pipeline/video_analyzer.py
</context>

<interfaces>
<!-- Existing interfaces the executor needs -->

From knowledge/pipeline/video_state.py:
```python
class VideoEntry(TypedDict):
    video_id: str
    title: str
    url: str
    duration: int  # seconds
    status: str  # pending | analyzed | extracted | failed
    analysis_file: str | None
    extracted_file: str | None
    analyzed_at: str | None
    error: str | None

def load_state(path: Path) -> dict
def save_state(state: dict, path: Path) -> None
def mark_video(state: dict, video_id: str, status: str, **kwargs) -> None
def get_videos_by_status(state: dict, status: str) -> list[dict]
def get_state_summary(state: dict) -> dict
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curate video lists and create secondary_videos.py</name>
  <files>knowledge/pipeline/secondary_videos.py, tests/test_secondary_state.py</files>
  <behavior>
    - Test 1: TOMALLSOPP_FOREHAND_VIDEOS has 40-50 entries, each with video_id, title, duration keys
    - Test 2: FEELTENNIS_FOREHAND_VIDEOS has 40-50 entries, each with video_id, title, duration keys
    - Test 3: No video_id appears in both TOMALLSOPP and FEELTENNIS lists
    - Test 4: Known FTT overlap video "1-g1OD8gh-I" is NOT in either secondary list
    - Test 5: generate_tomallsopp_state() returns dict with channel_id, total_videos, videos keys; all videos have status="pending"
    - Test 6: generate_feeltennis_state() returns dict with same schema; all videos have status="pending"
    - Test 7: Each video entry in generated state matches VideoEntry TypedDict schema
    - Test 8: Feel Tennis paid content video "CLEjGDGEGaA" is NOT in FEELTENNIS list
  </behavior>
  <action>
    Run `yt-dlp --flat-playlist --print "%(id)s|%(title)s|%(duration)s" "https://www.youtube.com/@TomAllsopp/videos"` and same for `@feeltennis`.

    Filter each channel's output for forehand-relevant videos using title keywords: "forehand", "kinetic chain", "rotation", "lag", "contact point", "topspin", "swing path", "weight transfer", "wrist", "racket drop", "follow through", "grip", "ready position", "unit turn", "hip rotation", "shoulder rotation", "arm slot", "windshield wiper", "pronation", "supination", "whip", "loading", "coiling". Exclude videos with "volley" or "serve" only in title (but keep if "forehand" also present). Keep ~45 per channel (prioritize technique over beginner/review).

    Create knowledge/pipeline/secondary_videos.py with:
    - TOMALLSOPP_FOREHAND_VIDEOS: list[dict] — hardcoded curated list, each entry = {video_id, title, duration}
    - FEELTENNIS_FOREHAND_VIDEOS: list[dict] — same format
    - FTT_OVERLAP_IDS: set = {"1-g1OD8gh-I"} — cross-channel duplicates to skip
    - PAID_CONTENT_IDS: set = {"CLEjGDGEGaA"} — paid content to skip
    - generate_tomallsopp_state() -> dict: builds state dict using VideoEntry schema, channel_id="@TomAllsopp"
    - generate_feeltennis_state() -> dict: builds state dict using VideoEntry schema, channel_id="@feeltennis"

    Both generate functions should use the same state dict schema as video_state.py's generate_initial_state(): {channel_id, total_videos, enumerated_at, videos: {video_id: VideoEntry}}.

    Write tests FIRST, then implement. Ensure FTT overlap and paid content are excluded.
  </action>
  <verify>
    <automated>cd /Users/qsy/Desktop/tennis && python -m pytest tests/test_secondary_state.py -x -v</automated>
  </verify>
  <done>Two curated video lists exist with ~45 videos each, no cross-channel duplicates, no FTT overlap, no paid content. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Generate state JSON files for both channels</name>
  <files>knowledge/state/tomallsopp_video_state.json, knowledge/state/feeltennis_video_state.json</files>
  <action>
    Create a small script (inline or in secondary_videos.py as __main__) that:
    1. Calls generate_tomallsopp_state() and saves to knowledge/state/tomallsopp_video_state.json via video_state.save_state()
    2. Calls generate_feeltennis_state() and saves to knowledge/state/feeltennis_video_state.json via video_state.save_state()
    3. Prints summary: channel name, total videos, all pending

    Run the script. Verify both JSON files exist and have the expected number of videos. Verify load_state() can read them back and get_state_summary() shows all pending.
  </action>
  <verify>
    <automated>cd /Users/qsy/Desktop/tennis && python -c "
from knowledge.pipeline.video_state import load_state, get_state_summary
from pathlib import Path
ts = load_state(Path('knowledge/state/tomallsopp_video_state.json'))
fs = load_state(Path('knowledge/state/feeltennis_video_state.json'))
ts_sum = get_state_summary(ts)
fs_sum = get_state_summary(fs)
assert 35 <= ts_sum['total'] <= 55, f'TomAllsopp count {ts_sum[\"total\"]} out of range'
assert 35 <= fs_sum['total'] <= 55, f'Feel Tennis count {fs_sum[\"total\"]} out of range'
assert ts_sum.get('pending', 0) == ts_sum['total'], 'Not all TomAllsopp pending'
assert fs_sum.get('pending', 0) == fs_sum['total'], 'Not all Feel Tennis pending'
print(f'TomAllsopp: {ts_sum}')
print(f'Feel Tennis: {fs_sum}')
print('OK')
"</automated>
  </verify>
  <done>Both state JSON files exist in knowledge/state/, contain ~45 videos each, all with status=pending, and are loadable by the existing video_state module.</done>
</task>

</tasks>

<verification>
- Both state files loadable by existing load_state/get_state_summary
- No video_id overlap between TomAllsopp and Feel Tennis lists
- No video_id overlap with FTT state (especially 1-g1OD8gh-I)
- All tests pass
</verification>

<success_criteria>
- knowledge/pipeline/secondary_videos.py exists with curated lists (~45 per channel)
- knowledge/state/tomallsopp_video_state.json has ~45 pending videos
- knowledge/state/feeltennis_video_state.json has ~45 pending videos
- tests/test_secondary_state.py passes with all assertions
- No cross-channel or FTT duplicate video IDs
</success_criteria>

<output>
After completion, create `.planning/phases/06-secondary-sources/06-01-SUMMARY.md`
</output>
