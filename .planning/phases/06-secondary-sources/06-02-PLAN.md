---
phase: 06-secondary-sources
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - knowledge/state/tomallsopp_video_state.json
  - knowledge/extracted/tomallsopp_videos/
  - docs/research/tomallsopp_video_analyses/
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "All ~45 TomAllsopp forehand videos have been analyzed by Gemini API"
    - "Raw analysis markdown is saved per-video in docs/research/tomallsopp_video_analyses/"
    - "Structured concepts are extracted and saved as JSON in knowledge/extracted/tomallsopp_videos/"
    - "State file tracks each video as analyzed or extracted"
  artifacts:
    - path: "docs/research/tomallsopp_video_analyses/"
      provides: "Raw Gemini markdown analysis per video (~45 files)"
    - path: "knowledge/extracted/tomallsopp_videos/"
      provides: "Structured concept JSON per video (~45 files)"
    - path: "knowledge/state/tomallsopp_video_state.json"
      provides: "State tracking with all videos marked analyzed/extracted"
  key_links:
    - from: "knowledge/pipeline/video_analyzer.py"
      to: "Gemini API"
      via: "analyze_batch() with 20s delay"
      pattern: "analyze_batch.*tomallsopp"
    - from: "knowledge/pipeline/video_concept_extractor.py"
      to: "knowledge/extracted/tomallsopp_videos/"
      via: "extract_concepts_from_analysis + save_video_extraction"
      pattern: "save_video_extraction.*tomallsopp"
---

<objective>
Analyze all curated TomAllsopp forehand videos (~45) via Gemini API and extract structured concepts.

Purpose: Generates the raw analysis and structured concepts needed for cross-source reconciliation in Plan 04.
Output: ~45 markdown analyses + ~45 JSON extraction files + updated state tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-secondary-sources/06-RESEARCH.md
@.planning/phases/06-secondary-sources/06-01-SUMMARY.md

@knowledge/pipeline/video_analyzer.py
@knowledge/pipeline/video_concept_extractor.py
@knowledge/pipeline/video_state.py
@knowledge/pipeline/secondary_videos.py
@config/youtube_api_config.json
</context>

<interfaces>
<!-- Existing interfaces from Phase 3 pipeline -->

From knowledge/pipeline/video_analyzer.py:
```python
def load_api_config(config_path: Path) -> dict
def create_client(config: dict) -> genai.Client
def analyze_batch(client, video_ids, prompt, state, state_path, delay=20.0, output_dir=None) -> list[tuple[str, str]]
def load_analysis_prompt(prompt_path: Path | None = None) -> str
```

From knowledge/pipeline/video_concept_extractor.py:
```python
def extract_concepts_from_analysis(analysis_text, video_id, video_title, registry) -> tuple[list[Concept], list[Edge], list[DiagnosticChain]]
def save_video_extraction(video_id, concepts, edges, chains, output_dir) -> Path
```

From knowledge/pipeline/video_state.py:
```python
def load_state(path: Path) -> dict
def save_state(state: dict, path: Path) -> None
def mark_video(state: dict, video_id: str, status: str, **kwargs) -> None
def get_state_summary(state: dict) -> dict
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Analyze TomAllsopp videos via Gemini API and extract concepts</name>
  <files>knowledge/state/tomallsopp_video_state.json, docs/research/tomallsopp_video_analyses/, knowledge/extracted/tomallsopp_videos/</files>
  <action>
    Read knowledge/pipeline/secondary_videos.py to get TOMALLSOPP_FOREHAND_VIDEOS list.

    Write a batch processing script (can be inline Python in execution) that:

    1. Load API config from config/youtube_api_config.json
    2. Create Gemini client via create_client()
    3. Load analysis prompt via load_analysis_prompt()
    4. Load TomAllsopp state from knowledge/state/tomallsopp_video_state.json
    5. Get list of pending video IDs from state
    6. Call analyze_batch() with:
       - video_ids = pending TomAllsopp IDs
       - delay = 20.0 (conservative rate limit)
       - output_dir = Path("docs/research/tomallsopp_video_analyses")
       - state_path = Path("knowledge/state/tomallsopp_video_state.json")
    7. After batch completes, load the existing concept registry snapshot from knowledge/extracted/_registry_snapshot.json
    8. For each successfully analyzed video:
       - Read the saved markdown from docs/research/tomallsopp_video_analyses/{video_id}.md
       - Call extract_concepts_from_analysis() with source tag "tomallsopp" (modify the source_file variable to use "tomallsopp_video_{video_id}" instead of "ftt_video_{video_id}")
       - Call save_video_extraction() to knowledge/extracted/tomallsopp_videos/
       - Mark video as "extracted" in state
    9. Save final state

    IMPORTANT: The source tag in extracted concepts should be "tomallsopp" not "ftt". When calling extract_concepts_from_analysis(), the function uses source_file = f"ftt_video_{video_id}" internally. To work around this without modifying the shared extractor, either:
    - (a) Pass source info via the video_title parameter: prefix with "[TomAllsopp] "
    - (b) Post-process the extracted concepts to fix source_file field
    - (c) Add an optional source_tag parameter to extract_concepts_from_analysis (preferred if clean)

    Process in batches of ~15 if the full ~45 is too many for one session. The state checkpointing means it's safe to resume.

    If any videos fail, log them but continue. Expect 90%+ success rate based on Phase 3 experience.
  </action>
  <verify>
    <automated>cd /Users/qsy/Desktop/tennis && python -c "
from knowledge.pipeline.video_state import load_state, get_state_summary
from pathlib import Path
import os

state = load_state(Path('knowledge/state/tomallsopp_video_state.json'))
summary = get_state_summary(state)
print(f'State summary: {summary}')

# Check analysis files exist
analysis_dir = Path('docs/research/tomallsopp_video_analyses')
md_files = list(analysis_dir.glob('*.md')) if analysis_dir.exists() else []
print(f'Analysis files: {len(md_files)}')

# Check extraction files exist
extract_dir = Path('knowledge/extracted/tomallsopp_videos')
json_files = list(extract_dir.glob('*.json')) if extract_dir.exists() else []
print(f'Extraction files: {len(json_files)}')

# At least 80% should be extracted
total = summary['total']
extracted = summary.get('extracted', 0)
analyzed = summary.get('analyzed', 0)
success = extracted + analyzed
assert success >= total * 0.8, f'Only {success}/{total} succeeded'
print(f'Success rate: {success}/{total} = {success/total:.0%}')
print('OK')
"</automated>
  </verify>
  <done>At least 80% of TomAllsopp videos analyzed and extracted. Raw markdown in docs/research/tomallsopp_video_analyses/, structured JSON in knowledge/extracted/tomallsopp_videos/, state file updated.</done>
</task>

</tasks>

<verification>
- State file shows majority of videos as "analyzed" or "extracted"
- Markdown analysis files exist in docs/research/tomallsopp_video_analyses/
- JSON extraction files exist in knowledge/extracted/tomallsopp_videos/
- No cross-contamination with FTT state or Feel Tennis state
</verification>

<success_criteria>
- 80%+ of ~45 TomAllsopp videos successfully analyzed and extracted
- Raw analysis markdown saved per-video
- Structured concepts saved as JSON per-video
- State file accurately reflects processing status
</success_criteria>

<output>
After completion, create `.planning/phases/06-secondary-sources/06-02-SUMMARY.md`
</output>
